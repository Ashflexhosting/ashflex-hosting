# Session notes — mobile menu bug diagnosis (Aug 19, 2026)

## Current request
User: "The mobile menu is still having problems" (no further detail).

## Diagnosis findings
The live production site (https://ashflexweb-pzcsotak.manus.space/) renders EMPTY below the navbar — the page content is missing entirely. Dev-server preview renders fine; production build shows a blank hero/nav only. This suggests the bug user sees on mobile may be related to production build issues, possibly dark-mode-related (production screenshot shows dark navy empty page with "Switch to dark mode" toggle visible).

Mobile menu code in client/src/components/Navbar.tsx (lines 273-445):
- Toggle button (line 288-295): `lg:hidden`, z-normal, inside nav z-[60], works via setMobileOpen.
- Backdrop: fixed inset-0 z-[55] navy/40 blur, onClick closes.
- Panel: fixed inset-x-0 top-0 bottom-0 z-[65] overflow-hidden, framer spring slide-in.
- swipeRef with pointerdown/move handlers closes menu on horizontal swipe >48px with threshold Math.abs(dx) > Math.abs(dy)*1.6. Panel scroll container has `touch-pan-y`.
- Body overflow hidden when open.

## UPDATE (15:35): production site is HEALTHY — full content renders (11350px tall, hero 676px, h1 229px). The blank screenshot was a transient load state. So the user's mobile-menu complaint is about the menu interaction itself.

## Confirmed issues / fixes to apply
1. PRODUCTION BLANK CONTENT — likely unrelated to mobile menu but MUST fix: check production logs (`manus-webdev-logs`) and browserConsole.log on live site. Could be a missing asset or hydration issue.
2. Mobile menu robustness improvements to make: (a) add touch-action: pan-y only on the scrollable list not whole panel; (b) increase swipe threshold to 80px to avoid accidental closes during scroll; (c) ensure panel has pointer-events auto (it does by default, but verify no `overflow-hidden` parent clipping).
3. Possible user-reported problem: earlier sessions fixed mobile menu bugs (scroll accessibility, slide-in animation, dark mode toggle). The "still having problems" likely refers to: (i) menu toggle not responding, (ii) menu content invisible (white text on white / dark-on-dark), or (iii) can't scroll the menu list.
4. Dark mode interaction: mobile panel uses hardcoded white/amber text — fine in both modes. But the toggle (X close button) at line 340-347 sits inside panel header.

## Test findings (emulated mobile layout in desktop browser)
1. Toggle opens menu OK; backdrop + panel render; body overflow hidden works; 14 links render; z-index correct.
2. Simulated scroll gesture (dy=300, dx=20) with threshold >48 && dx > dy*1.6 => NOT dismissed (correct, drift ratio too small).
3. After gesture, `links[0].click()` navigated OK — but `menuClosedAfterGesture:true` because window.location changed (navigate effect closes menu). Valid.
4. Production site healthy. So remaining realistic bugs on real phones: (a) on touch devices a fast fling-scroll can exceed 48px horizontal before vertical builds → false close; (b) `touch-pan-y` on whole scroll container + framer spring can fight native scroll momentum; (c) no inertial-safe handling. Also earlier user complaints: "cannot access menu when scrolling up" — the toggle button may sit under page content when scrolled on some pages? No — z-[60] fixed nav should stay on top. BUT: on some pages (e.g., pages using the same Navbar), if a section has a very high z or the page's scroll position puts nav under a full-screen hero overlay... verify per page: /services showed nav fine.
5. Fix plan: (1) raise swipe threshold 48→90px and ratio 1.6→2.0 to avoid false closes; (2) keep touch-pan-y but also ensure the swipe handlers don't swallow clicks: check pointercancel; (3) add `scroll-behavior` and ensure panel `overscroll-behavior-y: contain` to prevent body scroll bleed-through while menu open; (4) add `will-change` removal; (5) add visible focus and keep toggle button with bg contrast when scrolled (it already has text-white — on light pages after scroll scrolled=true → hover:bg-muted text-foreground/70, fine).
6. Also verify: menu scroll container `h-full overflow-y-auto` inside panel `overflow-hidden` — fine.

## Fixes APPLIED to Navbar.tsx (in progress, not yet checkpointed)
1. Swipe threshold raised: 48px→90px, ratio 1.6→2.0 (line ~96)
2. Menu scroll container: added `overscroll-contain` class (line ~355)
3. Added debounced `toggleMobile()` using openTimerRef (350ms guard) — prevents double-tap mid-animation race (toggle button, X close button, backdrop all use it)
4. useEffect cleanup clears timer on unmount
Remaining: run tsc + tests, screenshot verify at 375px, update todo.md, checkpoint, report. Live = checkpoint a8329a73.

## Investigation round 2 (user: problem started with dark/light switcher; suggests removing menu bg image)
KEY INSIGHT found in Navbar.tsx:
- Line 293: mobile toggle row uses `flex items-center gap-1.5 lg:hidden` with the theme button THEN the hamburger. But the NAV BAR ITSELF (line 148-155): when NOT scrolled it is `bg-transparent`. When scrolled: `bg-white/80 ... dark:bg-[#071633]/85`.
- CRITICAL: `@custom-variant dark (&:is(.dark *));` (index.css line 4) — the dark variant applies to .dark * (descendants). The ThemeProvider toggles class `.dark` on <html>. On mobile the nav bar when not scrolled is transparent; toggle buttons are `text-white` when not scrolled.
- When user switches to DARK theme: body becomes dark (--background dark navy), text becomes light. BUT the mobile hamburger button when NOT scrolled stays `text-white` — fine over dark bg. When scrolled on dark theme, nav becomes `dark:bg-[#071633]/85` with `hover:bg-muted` (dark muted oklch 0.24) and `text-foreground/70` (light text) — still fine.
- HOWEVER the mobile MENU PANEL background: the spotlight JPG + warm glow overlay (lines 340-353) — if the image fails to load or the warm overlay + theme CSS clash, the white text could become invisible. ALSO: panel header border `border-amber-400/25`, links white text.
- SUSPECT: When `.dark` is set on <html>, the `.dark` prefix on nav classes triggers via `(.dark *)`. But the panel is fixed-position at z-65 inside <nav> — fine.
- Another real candidate: `backdrop-blur-[3px]` on the swipe container combined with the bg image can make iOS Safari treat taps as hover gestures or delay clicks (blur on scrolling div). On iOS, `backdrop-blur` + `-webkit-overflow-scrolling` sometimes makes first tap trigger hover, second tap = click.
DECISION (per user): (1) REMOVE the background image + glow overlay from the mobile panel — replace with a clean solid navy gradient bg (dark bg-gradient-brand with noise); (2) remove backdrop-blur on swipe container; (3) keep white/amber link styling; (4) simplify theme toggle: keep it outside menu; ensure toggle's tap doesn't propagate (it's separate button, fine).
ALSO add: `fast-tap` — on iOS, add `touch-action: manipulation` to interactive elements so double-tap zoom doesn't interfere with taps.
Screenshots so far (375px): nav renders fine in light mode, toggle buttons visible top-right.

## FIX COMPLETE (round 3): MobileMenu extracted & verified
- DONE: MobileMenu component exported from Navbar.tsx; Navbar takes optional onMenuOpen/onMenuClose props, fires them via useEffect on mobileOpen; AppLayout in App.tsx owns state, renders Navbar + MobileMenu; panel z-[71] backdrop z-[70] now render after Footer/WhatsApp/StickyCTA in DOM.
- Leftover note: old swipeRef at Navbar line 111 is now DEAD CODE (swipe moved to MobileMenu) — remove it and its effect; also old keyboard handler at line ~141 in Navbar effect on mobileOpen (lines 114-156 effect uses swipeRef — dead). Remove the effect using swipeRef in Navbar (keep body overflow + debounced toggle effects).
- Screenshots at 375px show navbar area not visible at top of full-page shot (viewport only, need scroll up or crop) — navbar is fixed so fine; verify with browser if needed.
- tsc clean, 43/43 tests pass. Remaining: remove dead swipeRef in Navbar, checkpoint, report.

## FIX IN PROGRESS (round 3): extracted MobileMenu component
- Implemented: exported `MobileMenu` component in Navbar.tsx (after Navbar export) with props {open, onClose}; it renders backdrop z-[70] + panel z-[71] at the END of the component file. Removed the old AnimatePresence menu block from inside <nav>. Kept swipe/Escape handlers, debounced toggle (openTimerRef 350ms) still inside Navbar with toggleMobile().
- NEXT STEPS: (1) In App.tsx, import { MobileMenu } from ./components/Navbar, add state via Navbar callback — simplest: lift state in App.tsx: add useState mobileOpen false, pass onOpen/onClose callbacks to Navbar (need to modify Navbar props: add onOpen, onClose props replacing internal setMobileOpen for open/close — keep internal state but expose callbacks). Actually simplest: Navbar keeps its own state + MobileMenu reads state via a shared React context OR pass a callback: add Navbar props `{onOpen?: ()=>void, onClose?: ()=>void}` and Navbar calls them when opening/closing, and MobileMenu in App.tsx has its own open state? NO — state must be one source. Cleanest minimal: add context-free prop pair `open={mobileOpen}` won't work since state is internal. => CHANGE: Navbar takes `onOpen`/`onClose` callbacks it calls alongside internal setMobileOpen; MobileMenu in App.tsx mirrors state via the SAME callbacks? Still two sources of truth — fine since both toggled only via Navbar buttons. OR: add prop `onToggle` only — ambiguous for backdrop close. DECISION: Navbar accepts optional props `onMenuOpen?: ()=>void` / `onMenuClose?: ()=>void` and calls them whenever mobileOpen becomes true/false (useEffect on mobileOpen). App.tsx adds its own state mobileOpen + Navbar passes callbacks that set it. Menu closes via either Navbar close button or MobileMenu backdrop — both trigger onClose.
- (2) App.tsx render order: <Navbar />, ...pages..., <WhatsAppButton />, <StickyCTA />, <MobileMenu open={mobileOpen} onClose={() => setMobileOpen(false)} />
- Also note: MobileMenu component uses useLocation inside (imports ok). location change closes: useEffect(()=>setMobileOpen(false),[location]) inside Navbar; App's MobileMenu needs same — add useEffect inside MobileMenu? Navbar's effect will set its state false + trigger onClose callback — works since App's state follows callbacks. But if backdrop closes via MobileMenu, Navbar's internal state must also update — MobileMenu's onClose should trigger Navbar's onClose callback (already does) — good.
- (3) Run tsc + pnpm test, screenshot 375px verify, checkpoint, report.

## Investigation round 3 (user: menu opens but gets hidden BEHIND page sections when scrolled)
DIAGNOSIS: In Navbar.tsx the <nav> element itself has z-[60] and contains the menu panel (z-[65]). Navbar sibling order in App.tsx: Navbar, ScrollToTop, RestoreRedirectedPath, Router, Footer, WhatsAppButton(z-40), StickyCTA(z-30). Page sections themselves have NO z-index — static stacking. BUT the user reports menu goes behind sections when page is scrolled. Real cause candidate: the nav's `bg-transparent` when not scrolled — when scrolled it gains bg/blur, and its own background becomes opaque, so content under the NAV appears covered. However the PANEL extends full-screen z-[65] inside nav — should cover.
ACTUAL LIKELY BUG: The .backdrop div has z-[55] and the panel z-[65] — both inside <nav z-[60]>. But page sections that use `position: sticky` (FAQ page has sticky z-30; Services page has sticky filter z-50 at line 858!) — Services sticky filter z-50 < z-65, still below panel. Hmm, but what about `backdrop-blur` elements? A fixed section with `backdrop-blur` could...
KEY REAL CAUSE (paint order): fixed elements with SAME z stack in DOM order. Menu is INSIDE <nav>. When user scrolls, the .container page content is normal flow. The fixed panel at z-65 should paint over everything. EXCEPT: if the <nav> has `backdrop-blur-xl` when scrolled, its own layer paints, then panel above. This should still work.
ALTERNATIVE REAL CAUSE: the user may be describing that menu opens BUT the NAV bar background covers it because nav is z-[60] and menu panel z-[65] — panel > nav, so panel should paint above nav. Unless... `AnimatePresence` renders <> fragments; fixed divs inside nav with z-65 ARE higher. 
DECISION (pragmatic robust fix): remove ALL z-index from the menu elements AND the nav; render backdrop and panel as fixed divs OUTSIDE/after the <nav> in DOM (they already are inside nav but after the top bar). Raise panel to z-[100] and backdrop z-[95], and lower nothing else — but the true fix per standard practice: place backdrop + panel as PORTAL or after Footer in App.tsx when mobileOpen, completely outside nav's stacking context. Simplest: move the AnimatePresence menu block to render AFTER <StickyCTA> in App.tsx via a prop/hook — OR keep in Navbar but ensure nav z-[60] + panel z-[65] stays topmost; also remove z-50 from Services sticky filter? (z-50<z-65 fine)
CHOOSE: move mobile menu out of Navbar into App.tsx as a standalone overlay rendered after WhatsAppButton/StickyCTA (highest DOM order + z-70) controlled by a callback from Navbar (pass setMobileOpen + mobileOpen state up). This definitively breaks any stacking-context inheritance.
Also check: mobile menu nav links text color — white on navy ok.

## Key facts
- Project: /home/ubuntu/ashflex-agency (web-db-user, auto-publish enabled)
- Checkpoint a8329a73 = latest live. 43/43 tests pass.
- Dev server: http://127.0.0.1:3000 (Vite proxy), prod: ashflexweb-pzcsotak.manus.space
- StickyCTA: fixed bottom z-30; WhatsAppButton: fixed bottom-right z-40; Navbar: z-[60]; menu panel z-[65]
- TODO items pending (in todo.md under "Mobile Menu Bug Fix"): reproduce/diagnose, fix, verify, checkpoint, report

## NEW REQUEST (16:40): "Check the pricing page; it's not mobile-friendly"
375px full-page audit (file pricing_part1/2/3.png):
1. TIER CARDS worst issue: cards render full-bleed edge-to-edge; plan subtitle truncates off right edge; "1 Year Free Hosting + Domain (.com / .com.ng / .n" overflows beyond card edge; card content not contained within padding.
2. FEATURE COMPARISON TABLE: mobile stacked single-column — feature label dark block + value to the right; only one plan's value shown per row (other plans missing? likely horizontal scroll). Desktop-only hint text "Hover over any plan card above..." — wrong wording on touch.
3. Get Started buttons in table: small outline buttons, check tap targets.
4. Rest (What Happens Next, custom solution CTA, footer) OK.
Plan: fix Pricing.tsx tier card containment + text wrapping; ensure comparison table keeps horizontal scroll with sticky Business column; fix hint wording; verify tap targets. Then tsc + tests + checkpoint.

## PRICING MOBILE FIX — STATUS
Fixes applied to client/src/pages/Pricing.tsx:
1. Tier cards: replaced horizontal scroll strip (flex + snap) with grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-4 gap-6 — cards now stack full-width on mobile and sit properly within container padding. Verified in 375px viewport screenshot: Starter card renders fully contained, no overflow.
2. Feature text wrapping: added min-w-0 + break-words to feature li spans (both included and excluded items) so long text (hosting line) no longer overflows card edge.
3. Comparison summary hint: "Hover over any plan card..." → "Tap or hover on a plan in the table below..." (touch-appropriate).
4. Comparison table: Business column now sticky right on ALL widths (removed md: prefix), added mobile hint "Scroll horizontally to compare all plans" with pulsing arrow.
tsc clean, 43/43 tests pass. Remaining: verify final screenshot (captured, looks good — cards contained), checkpoint, report.
NOTE: earlier viewport 375 screenshots were rendered at 1279px scale (full_page) — use full_page=false to verify true width. The latest 375x812 (non-full-page) shows hero + starter card perfectly contained.

## COMPARISON TABLE SCROLL BUG (user: "horizontal scroll on the plans comparison looks broken")
Diagnosis: At desktop width the page renders in DARK MODE — this is a screenshot artifact only (user's own browser likely). Real issue: in the browser at this width (1279px viewport in browser), the table header renders "Feature | Starter | Professional | Business | Enterprise" — Business column appears between Professional and Enterprise, BUT the header columns render at 280px widths → table 170+280+280+270+280 = 1280px = min-w-[1280px]. At 1279px viewport the wrapper overflow-x-auto becomes scrollable even at desktop → table is "broken" (jiggly layout, horizontal scrollbar on desktop where it shouldn't scroll). Also Business column shows BEFORE Enterprise in DOM but sticky right — header shows "Professional | Business | Enterprise" while table data shows Business sticky-right.
Hypothesis for "broken": at mobile widths (375), the table width 1280px should scroll. But user says broken — likely the table IS scrolling at desktop sizes too (1280 > 1279/1200), OR sticky columns overlap/misalign. FIX: reduce min-w to 1000px (200*4+200) so at mobile 375 it scrolls but at tablet+ (768+) it fits; change column widths: feature 160, plans 210 each → 160+210*4=1000. Check mobile view after.

## COMPARISON TABLE BUG — DETAILED (mobile 375px crop)
Findings from crop of mobile screenshot:
1. Sticky Business column (270px, sticky right, z-20) paints ABOVE its own row cells — we only see "Business" header + 2 columns (Pages: 10 / 5) when scrolled to right edge; columns appear out of order (10 under Business then 5 under Enterprise). Actually the two visible columns are Business(sticky) + Enterprise.
2. Sticky left "Feature" column overlaps first plan column (only shows "Feature..." truncated text, and its width is broken — the sticky cell doesn't stretch to 170px).
3. Table min-w 1280px → huge scroll distance on mobile (scroll 905px to reach Enterprise). Broken feel.
4. Header shows only "Business" visible at right edge; other headers hidden under sticky.
Root cause: sticky positioned th cells with explicit w-[170px]/w-[270px] but table cells don't get explicit width → widths collapse; also sticky left/right on <th> within a scrolled container works but column widths lost because table-layout auto.
FIX PLAN:
- Set table-layout: fixed on the table.
- Give each column a consistent explicit width (colgroup or w-* on th+td).
- Reduce min-w-[1280px] → responsive: min-w-[1000px] sm:min-w-[1000px] so at mobile scroll is manageable (1000px) and Business still sticky.
- Make sticky cells match width of non-sticky via w-[160px] feature + w-[210px] each plan.
- Ensure td cells also carry same w- classes (tds currently lack width).

## TABLE FIX ROUND 2 FINDINGS
After table-fixed + colgroup (170/210/210/200/210 = 1000px):
- Scroll distance much better. BUT at the right-scroll edge the Business sticky (z-20) still overlaps the Enterprise column visually ("Advanced" vs "Basic" overlap, "2 weeks" vs "week", buttons overlap). Wait — the screenshot shows the RIGHT edge of scroll. Business is sticky-right, Enterprise is at the far right column beyond Business. The overlapping text shows two values side by side: "Advanced/Basic" — this is Business cell + Enterprise cell visible at the very end of scroll. Actually overlap = text bleeding beyond 200px column. "Advanced" belongs to Professional? No: row order Starter/Professional/Business/Enterprise. At right edge we should see Business + Enterprise. "Advanced" appears with "Basic" — meaning the Professional (Advanced) and Business (Basic) columns both visible... overlapping? Hmm.
- More likely: sticky right cell width (colgroup 200px) doesn't stretch its content area to match neighbors (210px), so adjacent column's content overlaps it. The visible "AdvancedBasic" suggests cells overflow: text "Advanced" centered in 210px column + "Basic" in 200px sticky column, both rendered but sticky column sits ON TOP of the Professional column? No — sticky right stays at right edge of container, so when scrolling near end, we see Business(sticky) + last column visible = Enterprise. But text shown is "Advanced"(Professional) + "Basic"(Business). That means Business sticky is positioned over the PROFESSIONAL column area? At this scroll position the right-most non-sticky column visible next to Business would be Enterprise... unless the table width shrunk.
- Actually likely cause: sticky right on th/td within table-fixed works, but the sticky Business column z-10/20 has bg-brand-secondary/5 while header uses bg-background. The overlapping rows show "Advanced/Basic" = columns 2 (Professional, value Advanced) and 3 (Business, value Basic) — so at this scroll position Professional is right next to Business: scroll is near the MIDDLE, and Business column overlaps Professional because Business (z-20 header) paints over Professional. The sticky right-0 means Business stays pinned at the right edge of the viewport. So what we see at right edge of viewport = Business sticky + whatever columns to its left peek out (Professional). That's CORRECT behavior. The "broken" look may just be the default scroll position in the screenshot tool showing the rightmost area.
- The Get Started row: buttons overlap (Business "Get Started" + Professional outline button both visible overlapping) — same cause: sticky Business overlaps Professional.
- So at the END of scroll: Business + Enterprise should both show. The screenshot captured by the tool appears to be scrolled to the far right where Professional + Business are visible?? The tool may scroll the container.
CONCLUSION: behavior is likely correct; overlap is natural at intermediate scroll positions. The prior broken state (table 1280px wide, columns collapsing) is fixed. Verify at 0 scroll position: should see Feature + Starter.

## CURRENT STATE (table fix)
Browser viewport is 893x768 desktop, NOT mobile. Need to verify table at 375px via browser using window.resizeTo not available in sandbox browser. The screenshot tool at 375x812 full-page is the reliable mobile check.
Fix applied to Pricing.tsx: table now table-fixed with colgroup widths 170/210/210/200/210 (total 1000px), min-w-[1000px], sticky Feature column left z-10, sticky Business column right z-20 (header) / z-10 (body). All per-cell w-[...] classes removed (colgroup controls widths).
Mobile crop 2 at 375px showed table renders with proper columns at right-scroll edge (Business + Enterprise visible). Overlap observed at edge is expected (Business sticky overlaps Professional when not at far end of scroll — correct sticky behavior).
Next: verify at default scroll position (should show Feature + Starter), run tsc+tests (already passed: 43/43), checkpoint, report.
TODO items to mark done after checkpoint: "Comparison Table Scroll Fix" section (added earlier as ## ... (u... truncated — check top of todo.md).

## HOSTING & DOMAIN TABLE LOCATION
The "Hosting & Domain" comparison table the user praises is in client/src/pages/ServiceDetail.tsx around line 245-290 (HostingComparisonTable component, inside the Hosting & Domain Packages section ~line 165). It has hosting-tier-cards + a comparison table with sticky Business column (line 270: "md:sticky right-0 z-20 w-[190px] bg-background"). Route: it's part of ServiceDetail (/services/:slug), so the user sees it on a service detail page.
Next: read ServiceDetail.tsx lines 240-330 to see the mobile design pattern (does it use table-fixed/colgroup like the pricing fix, or a different mobile flow the user likes).

## UPDATE: hosting table location
/servics/website-design shows "From ₦150,000" pricing card but NO hosting comparison table. /services/ecommerce = 404 (wrong slug). The HostingComparisonTable (ServiceDetail.tsx ~line 165 "Hosting & Domain Packages") only renders conditionally — check the condition at line ~160-180. Need to find which service slug shows the hosting table (maybe /services/hosting-domain or condition is on all services?). Check services data in client/src/data.

## HOSTING TABLE DESIGN THE USER LIKES (verified at /services/hosting-domain, 375px)
The user likes how the hosting-domain comparison table FLOWS on mobile: min-w-[640px] (vs pricing's 1000px) so only ~1.7 screens of swipe; auto table layout (no table-fixed) so columns shrink proportionally; the Feature column uses solid bg-brand navy with white text and is sticky left-0; the highlighted Business column header sticky right on md+; rows have a subtle white bg-brand left column and hover bg-muted/40; price row bold brand-secondary text; check icons in brand-success; overall feels "light and readable" (mostly light rows with checkmarks, not text-dense).
Pricing table differences: 1000px min width (too wide), table-fixed (rigid), Business column order is 3rd in DOM (Starter/Professional/Business/Enterprise) so swipe feels broken — on hosting it is the LAST column (Standard/Plus/Premium order has Business=Plus in the middle? No — hosting: Standard, Business?, Premium — check hostingTiers order in data).
Also pricing table has the Business column sticky right while placed 3rd of 4 → columns overlap at default scroll (Starter left sticky + Business right sticky + Professional/Enterprise hidden between). On hosting page, the table is 3 cols (Standard/Premium?) — verify hostingTiers count = 3.
PLAN: make Pricing comparison table flow like hosting: reduce min-w to ~680px, remove table-fixed (use auto layout w/ per-cell widths), keep Feature sticky left bg-brand white text, keep Business sticky right on md, rows light w/ hover tint, checkmarks. Reorder columns so Business is last (Starter, Professional, Enterprise, Business)? Or keep order but make Business not sticky on mobile until far edge — better: place Business LAST so natural swipe ends on recommended plan, like hosting (Standard → Business → Premium ends on Premium highlighted?). Verify hostingTiers order.

## hostingTiers order (client/src/data/services.ts line 152)
Order: Starter (₦60,000/yr) → Business? (need mid) → Premium. Three tiers total. Highlighted middle one = Business with star badge + pulse. Columns: Standard(Starter)/Business/Premium. Business highlighted and sticky right on md+. On mobile the natural swipe left reveals Business as the last/right column — the recommended plan sits at the END of the scroll, which feels like a natural "arrival" moment. That's the flow the user likes.
DECISION for pricing table: keep column order Starter/Professional/Business/Enterprise but swap DOM order to Starter/Professional/Enterprise/Business so the highlighted Business column is the LAST column (rightmost), matching the hosting table's flow. min-w reduced to 680px, remove table-fixed (auto layout), per-cell widths via w-[...] classes, Feature column sticky left bg-brand white text, Business sticky right md+ bg-brand-secondary/5 tint, rows light hover:bg-muted/40, checkmark icons brand-success, price row bold brand-secondary.
Pricing data: comparisonFeatures[] has starter/business/professional/enterprise fields. Plans: Starter ₦150,000, Business ₦350,000 (popular), Professional ₦750,000, Enterprise ₦1,500,000+.

## Pricing table rework debug (webdev_debug result, HIGH confidence)
Fix: reintroduce table-fixed + colgroup with widths Feature 170 / Starter 170 / Professional 170 / Enterprise 170 / Business 180 = 860px; set min-w-[860px]; remove per-cell w-* (or keep, colgroup wins under table-fixed anyway). Keep sticky left Feature z-10 bg-brand, sticky right Business z-20 header/z-10 body bg-brand-secondary/5 (opaque enough since tint on background ok). Header cells font var(--font-heading), star + badge-pulse on Business. Rows: border-b border-border/40 hover:bg-muted/40, py-3.5. Business column DOM-last (Starter, Professional, Enterprise, Business). Scroll hint text already: "Scroll to compare — the Business plan sits at the end". renderValue, RenewalTooltip, TimelineTooltip, GetStartedButton unchanged.
After fix: verify crop at 375px, tests 43/43, tsc, checkpoint.

## VERIFIED table_v4.png (mobile 375px, after table-fixed+colgroup 860px)
Comparison table renders correctly: sticky navy Feature column left, middle columns visible (Pages=10, E-commerce=Up to 50, SEO=Advanced...), sticky Business column right with pulse star and gradient CTA. No overlaps. Scroll hint present. Footer/What Happens Next fine. READY: mark todo, checkpoint, report.

## Overlap follow-up diagnosis (Aug 19, ~17:30)
Both debug agents converge: the full-page mobile screenshot captures the overflow-x container at a scrolled position (scrollLeft near max = Feature + Business visible). Middle columns Starter/Professional/Enterprise exist but are scrolled behind the sticky Business column — text bleed looks like overlap ("AdBasicized" = Advanced+Basic doubled). Root causes: (1) container starts scrolled right (browser persists scroll position / screenshots capture post-scroll); (2) middle cells have transparent backgrounds so text can visually double at sticky boundary; (3) sticky cells use translucent bg-brand-secondary/[0.04].
FIX PLAN: (a) add scrollLeft:0 on mount via ref + useEffect (also on window resize/after paint); (b) give all non-sticky cells explicit opaque bg (bg-background/transparent-white), make sticky Business cells fully opaque (bg-background w/ border-l shadow), add isolation:isolate on wrapper; (c) verify with a viewport-only (non full-page) 375px screenshot which starts at scroll left 0.

## Overlap fix status (Aug 19, ~17:45)
Applied to client/src/pages/Pricing.tsx (version ce004a0b base):
1. Added `tableScrollRef` + mount effect: forces scrollLeft=0 on the comparison table wrapper so the table always starts showing Feature + Starter columns, not scrolled to Business.
2. Middle th/td cells now have explicit bg-background; table is border-separate border-spacing-0 with bg-background; sticky Business th/td: bg-background + border-l border-border + shadow-[-10px_0_16px_rgba(15,23,42,0.08)]; removed translucent bg-brand-secondary/[0.04]; wrapper has isolation-isolate.
3. tsc clean, 43/43 tests pass.
IMPORTANT SCREENSHOT INSIGHT: full-page screenshots capture scrollLeft>0 (table shows only Feature + Business by design). The LATEST full-page crop (table_v5.png) now shows clean single-value rows (Pages=10, SEO=Advanced, E-commerce=Up to 50) with NO doubled text — only one plan column visible because it's scrolled to the right edge, which is EXPECTED behavior for a sticky-right-first-column table at scrollLeft=max. The earlier "AdBasicized" doubling is gone.
NEXT: verify with a browser at 375px by programmatically scrolling, or accept: need a way to show in screenshot that Starter column exists at left. Consider also verifying by taking screenshot with window.scrollTo? Full-page capture always starts at scrollLeft=0 of vertical, horizontal scroll state depends on browser... The v5 shot appears to be at scrollLeft=max, suggesting browser persisted or full-page capture scrolled horizontally?? That's odd — maybe the full-page capture mechanism scrolls to each section? Regardless, user's real complaint was doubling/overlap; doubling is gone. To be safe: also test with a scrollLeft=0 verification via console in browser OR check if full-page screenshots scroll horizontally... The earlier v4 screenshot also appeared scrolled to right (showed only 1 column) which suggests the screenshot tool captures horizontal scroll = max (maybe due to element scrollIntoView?). DECISION: open browser on live dev URL at 375px and inspect actual render; then checkpoint.

## Browser verification results (Aug 19, ~17:22)
DOM measurements at emulated 375px width: header columns at left edge — Feature 32-202 (170), Starter 202-372, Professional 372-542, Enterprise 542-712, Business pinned 227-407 (180). At right edge, Enterprise ends exactly at 227 = Business left: NO numeric overlap. Sticky position working, z-10 applied.
ISSUE FOUND: wrapper isolation computed as "auto" — the class "isolation-isolate" is NOT a valid Tailwind class (v4 uses "isolate"), so my edit didn't apply isolation. Also tableBg shows oklch dark navy (0.16 0.06 264) — the page was in DARK mode in this browser (theme persisted), so bg-background resolved dark.
ACTION: change "isolation-isolate" to "isolate" in Pricing.tsx; take desktop screenshot both light/dark; user's earlier complaint of doubling may have been at scrollLeft=max. Need a visual at true 375px in LIGHT mode with scrollLeft=0. Full-page screenshots at 375px appear to capture the table scrolled to the right (tool behavior?) — actually full-page capture probably scrolls page down section-by-section, and the horizontal scroll container may have been scrolled... The v5 crop showed rows like "SEO Setup: Advanced" at Business column edge — that looked like doubling but may be the single column at max scroll. With scrollLeft forced to 0 on mount, initial paint shows Feature+Starter which reads clearly.
DECISION: fix class to "isolate", verify dark mode too (check bg-background in dark theme on middle cells is opaque dark — yes dark mode .dark sets bg oklch ~0.16 opaque), screenshot again, checkpoint.

## Desktop verification (table_desktop.png)
At 1279px the comparison table renders perfectly: all 5 columns (Feature navy, Starter, Professional, Enterprise, Business with star + gradient CTA) side by side, no overlap, no doubled text. Rows like SEO Setup (Basic/Complete/Complete/Advanced), Free Support (1/6/12/3 months) all clean.
Emulated 375px DOM check: columns adjacent, no numeric overlap; sticky Business pinned right at both scroll extremes; scrollLeft forced to 0 on mount via ref effect; middle cells opaque bg-background; wrapper has `isolate` class (fixed from invalid isolation-isolate); sticky Business cells have border-l + shadow for separation.
tsc clean, 43/43 tests pass. REMAINING: mark todo items, checkpoint, report to user. The doubling the user saw was the translucent sticky Business cell letting middle-column text bleed through at the boundary — now opaque + shadowed.

## Scroll not working diagnosis (Aug 19, ~17:45)
Debug agent (medium confidence): on touch devices, swipes starting on sticky cells (z-index, wide) don't propagate pan gesture to the overflow-x wrapper. Also `isolate` (removed, now touch-pan-x) may hurt.
FIX: (1) add touch-action pan-x to the scroll wrapper AND to the sticky cells via inline style; (2) guard scrollLeft=0 to run once (use a guard ref); (3) simplest robust: only make the Business column sticky on md+ via md:sticky; keep right:0 sticky on md only.
DECISION: apply touch-action: pan-x on wrapper + sticky th/td (inline styles since Tailwind has touch-pan-x but needs sticky cells too), and reduce sticky Business to md-only is risky for UX — keep sticky everywhere but ensure pan-x works. Also ensure no re-run of mount effect (deps [] already, fine).

## Scroll verification results (Aug 19, ~17:35)
At emulated 375px: touchAction = pan-x on wrapper + sticky th/td (non-sticky cells auto). Programmatic scrollLeft works: scrollLeft=300 OK, scrollTo(600) clamps to maxScroll=485. The synthetic pointer-drag (PointerEvent) didn't move scrollLeft, but that's a desktop-Chromium limitation (pointer-drag isn't how desktop browsers scroll overflow-x-auto; only touch/wheel). On a real touch device, native touch scrolling should work now with touch-action: pan-x.
The wrapper responds to touch because it's overflow-x-auto; earlier it could have been blocked by the `isolate` stacking context or a parent touch-action. Fixed: isolate removed, touch-pan-x + inline touchAction: pan-x on wrapper and all sticky cells.
NOTE: The user reported on a REAL phone that scroll "is not working again". To fully verify on device I cannot; but the CSS/touch-action fix is the standard solution. Also verify no ancestor (section? page?) has touch-action blocking; check index.css for body touch rules.

## Final verification (Aug 19, ~17:38)
At desktop viewport the emulated 375px check returned scrollable:false — that's because the .container at full 1280px width is wider than the 860px table, so no scroll is needed there. At true mobile widths the container is 375px and scrollable:true. touchAction=auto confirmed on wrapper; ancestors all auto/visible; only the section has overflow=hidden (cosmetic clipping). scrollTo works both directions. WebkitOverflowScrolling: touch applied. Sticky left Feature + sticky right Business columns still in DOM with z-index.
CONCLUSION: native touch scrolling should now work on iOS/Android because: wrapper is overflow-x-auto with touch-action:auto (default) and -webkit-overflow-scrolling:touch, no blocking ancestors, no global touch preventDefault handlers. Checkpoint next.

## Sticky Business removal verification (Aug 19, ~18:15)
Edited Pricing.tsx: Business th/td/CTA changed from `sticky right-0 z-*` to `md:sticky md:right-0 md:z-*` with conditional `md:border-l md:border-border md:shadow-[...]`. Feature column stays sticky left on all sizes (no conflict — left sticky doesn't swallow the whole viewport).
Mobile 375px: crop (table_mobile_flow.png, wrong crop region — showed pricing cards, not the table; the table is lower in the page ~60% height) — need to re-crop lower region to confirm. Desktop: full page shows table renders with Business at right end as normal column (no sticky at desktop either since viewport >= md? desktop has md+ so Business IS sticky there — appears fine at the right edge).
tsc clean, tests pending. Remaining: re-crop mobile table at correct y (~0.58-0.70), verify, checkpoint.

## Resolution of sticky-check confusion (18:16)
The browser DOM check reported Business header "sticky" because the desktop browser is at 1440px viewport — md breakpoint keeps Business sticky at desktop, which is intentional. The 375px screenshot crop (table_mobile_v2.png) confirms on real mobile width the Business column is now a normal flowing column (no pinned left border/shadow, cells aligned naturally next to Enterprise), maxScroll = 485px and scrolling reached the end — the table scrolls freely on mobile.
Final behavior: mobile = free horizontal scroll, only Feature column sticky left; desktop (md+) = Business sticky right preserved.
