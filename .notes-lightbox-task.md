# Task notes: Gallery high-resolution lightbox (2026-08-13)

## User request
Add a lightbox effect to the portfolio gallery images so visitors can click to view the full-page captures in high resolution.

## Current state (already implemented before this request)
- PortfolioDetail.tsx (client/src/pages/PortfolioDetail.tsx) already has a lightbox at lines 172-223:
  - Gallery tiles (button, line 97-115) click → `setLightboxIndex(n)`; has "View full size" hover overlay.
  - Lightbox uses ScrollableScreenshot inside fixed dialog, `height="h-[75vh]"`, max-w 92vw, max-h 80vh, caption + counter, prev/next arrows + keyboard nav (Esc/ArrowLeft/ArrowRight), backdrop click closes.
  - So a lightbox exists; the ask is to make it feel HIGH-RESOLUTION full-page viewing.

## Plan
- Enhance lightbox: render image at natural resolution with zoom/pan:
  - Add a "Fit / Zoom" toggle or default to scrollable full-res view (ScrollableScreenshot supports any height; use h-[80vh] frame, image displayed at full natural width capped by container).
  - Optionally add a simple zoom-in button: CSS transform scale on click with drag pan, or pinch/scroll zoom on the image.
  - Keep caption+counter, arrows, keyboard, Esc close.
- Also check CaseStudyDetail.tsx gallery lightbox (similar pattern) and apply the same treatment if present.
- CaseStudyDetail.tsx exists with related case studies section (from context).

## Key facts
- Portfolio data: client/src/data/portfolio.ts — projects have `screenshots: [...]` + `screenshotCaptions: [...]`.
- ScrollableScreenshot component: client/src/components/ScrollableScreenshot.tsx (scroll hint, wheel auto-scroll on hover, accepts height prop e.g. h-[80vh]).
- Tests: pnpm test (24/24 passing). Auto-publish enabled — checkpoint publishes immediately.
- Latest checkpoint before this task: 51eec270.
