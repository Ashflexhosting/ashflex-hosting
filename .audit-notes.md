# Mobile audit (375x812) — findings

- /services: renders OK; category filter bar present, cards stack. No obvious squeeze. Sticky filter works per earlier fixes. Minor: banner gradient fine.
- /pricing: comparison table shows only two columns (Feature + Starter) at 375px — need horizontal scroll check; Business column sticky confirmed earlier. Tooltip icons and CTAs visible. OK but verify scroll.
- /portfolio: grid OK, filters OK. Project names readable.
- /portfolio/karossy-travels: "Project Not Found" — the slug is wrong. Need actual slugs from the portfolio data (grep portfolio items).
- Footer on mobile: brochure download appears twice (once beside contact card, once again at bottom). Check duplication — earlier request moved brochure beside contact card; a second "Download Our Brochure (PDF)" link below contact card on mobile may be duplicate per earlier design intent. Verify intent.
